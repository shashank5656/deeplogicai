# Invoice Extraction & Quality Control (Extractor + CLI)

This project implements a **PDF → JSON invoice extraction system** and a **command-line interface** for running extraction and validation workflows.  
It was built as part of the **DeepLogic AI — SDE Intern (Data & Development)** assignment.

---

## 📌 Project Status
- ✅ Core **extractor working** (PDF → text → structured JSON)  
- ✅ **CLI** implemented (extract, validate, full-run)  
- 🟡 **Validation rules** implemented partially  
- 🟡 **FastAPI service** (HTTP API) in progress  
- 🟡 README + documentation completed  

This repository already extracts real invoice fields into `output.json`.


